# T-004: ユーザー認証（F-01）

- **Status**: TODO
- **Phase**: 1 / **Track**: 基盤
- **依存**: T-001（Next.js）, T-002（Supabase プロジェクト）
- **要件**: 要件定義書 §4.2（F-01）
- **担当パス**: `app/login/**`, `middleware.ts`, `lib/supabase/**`, `app/layout.tsx`（ログアウトボタン部分）

## ゴール

Supabase Auth（メール＋パスワード）でログイン／サインアップ／ログアウトができ、未ログインでは保護ページに入れない状態にする。

## タスク

- [ ] `@supabase/ssr` を導入し、`lib/supabase/client.ts`（ブラウザ用）/ `lib/supabase/server.ts`（Server Components / Server Actions 用）を作成する
- [ ] `/login` ページを作成する（ログインとサインアップの切り替え、メール＋パスワード）
- [ ] `middleware.ts` で未ログイン時に保護ページ（`/`, `/mock/**`, `/study/**`, `/review/**`）から `/login` へリダイレクトする
- [ ] ログイン済みで `/login` にアクセスしたら `/` へリダイレクトする
- [ ] ヘッダーにログアウトボタンを実装する
- [ ] サインアップ時のメール確認の扱い（確認あり／なし）を決めて README に記載する

## 完了条件

- 未ログインで保護ページにアクセスするとログイン画面にリダイレクトされる
- サインアップ → ログイン → ログアウトが一通り動作する
- ログイン後のユーザー ID が Server Action から取得できる（後続チケットが利用する前提）

## 並行開発メモ

- `lib/supabase/**` は全チケットが使う共通基盤。完了後はシグネチャを変えないこと。
