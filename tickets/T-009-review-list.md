# T-009: 復習一覧画面（F-04）

- **Status**: TODO
- **Phase**: 4 / **Track**: C
- **依存**: T-002（スキーマ）, T-004（認証）
- **要件**: 要件定義書 §4.5（F-04）
- **担当パス**: `app/review/**`, `components/review/**` のみ

## ゴール

直近 10 回分の模擬テストセッションを一覧で振り返れる画面を作る。

## タスク

- [ ] `/review`: mode='mock' かつ完了済み（finished_at が非 null）のセッションを**新しい順に直近 10 件**表示する
- [ ] 各行に表示: 実施日時 / 正解率（%）/ 正解数・出題数 / 所要時間（started_at と finished_at の差）
- [ ] 行クリックで `/review/[sessionId]`（T-010）へ遷移する
- [ ] セッションが 0 件のときの空状態表示（「模擬テストを受ける」への導線）
- [ ] 11 件目以降は一覧に表示しない（データ削除はしない）

## 完了条件

- 直近 10 回分のセッションが新しい順に表示される
- 自分のセッションのみが表示される

## 並行開発メモ

- 読み取り専用の画面なので T-006（模擬テスト）と並行開発可能。動作確認用のセッションデータは、SQL で手動投入するか T-006 完成後に実際に受験して作る。
