# TT-004: 結果画面（正解率・問題別レビュー）

- **Status**: DONE
- **依存**: TT-003
- **担当パス**: `test-app/app/exam/result/**`, `test-app/components/exam/**`

## ゴール

模擬テスト終了後に正解率と問題ごとの正誤・解説を確認できる結果画面を作る。

## タスク

- [x] `/exam/result`（または TT-003 から渡されたデータを表示する同等のルート）を作成する
- [x] サマリー表示: 正解数 / 65、正解率（%）
- [x] 問題ごとの詳細リスト（全65問分）:
  - 問題文
  - 自分の回答（出題時のシャッフル後の並びで表示）
  - 正解
  - 正誤（正解／不正解のバッジなど）
  - 解説
  - 未回答の問題は「未回答（不正解扱い）」と明示する
- [x] 「もう一度挑戦」ボタンで `/exam` に戻り、最初から新しい65問でやり直せる
- [x] TT-003 からのデータが無い状態（直接 `/exam/result` にアクセスした等）の場合は `/exam` へ誘導する簡単な空状態を表示する

## 完了条件

- [x] 正解率・正解数がすぐに確認できる
- [x] 全65問について、自分の回答・正解・正誤・解説が確認できる
- [x] 選択肢が出題時と同じ並びで表示される
- [x] 「もう一度挑戦」で再度模擬テストを開始できる

## メモ

- DB・API Route は使わない。TT-003 から渡された state（または sessionStorage 等）をそのまま表示に使う。
- 本番版（`tickets/T-007`, `tickets/T-010`）と似た UI になるが、共通化は不要（このプロトタイプは `test-app/` 内で完結させる）。
